
const dataValidationController = (req, res) => {
    const chartType = req.body;

    //The data will be validated and
    //a response okay or not okay
    //will be sent to frontend
}

module.exports = dataValidationController;